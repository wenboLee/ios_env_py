

创建一个cli，实现以下功能：
支持 使用ssh 连接一个主机（可以配置具体的IP地址和端口，用户+密码），
执行 fastlane spaceauth -u your_email@example.com
这个邮箱也支持配置。
输入二次验证码之后，将 FASTLANE_SESSION 写入给主机的 $HOME/workspace/.env 文件。
更新原来的环境变量

.evn 文件中，还有其他变量，不希望修改其他变量